spring:
  application:
    name: video-agent
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-plus
  servlet:
    multipart:
      max-file-size: 10MB

server:
  port: 8080

logging:
  level:
    com.example.videoagent: DEBUG
    org.springframework.ai: DEBUG
